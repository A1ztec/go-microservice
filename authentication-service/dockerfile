FROM alpine:latest

# FIX: Set the working directory first
WORKDIR /app

# Now everything copies directly into /app automatically
COPY authApp .
COPY .env .

# Since WORKDIR is /app, this executes inside /app where .env lives
CMD ["./authApp"]